{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "watch": "tsc-watch --p . --onSuccess \"npm run compileBabel\" --onFailure \"echo 'TypeScript compilation failed' && exit 1\"",
    "build": "tsc",
    "deploy": "webpack",
    "typecheck": "tsc --p .",
    "compile": "cross-env-shell babel $INIT_CWD -d $INIT_CWD --extensions '.ts' --no-comments --source-maps --out-dir ./_IDE",
    "compileBabel": "babel src -d build --extensions '.ts' --no-comments --source-maps --out-dir ./_IDE --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck-compile": "npm run typecheck && npm run compile"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knockout": "^3.5.1",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.6",
    "@babel/core": "^7.18.6",
    "@babel/preset-env": "^7.18.6",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jquery": "^3.5.16",
    "babel-loader": "^9.1.2",
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "rimraf": "^5.0.1",
    "ts-deferred": "^1.0.4",
    "ts-node": "^10.9.1",
    "tsc-watch": "^6.0.4",
    "webpack": "^5.82.0",
    "webpack-cli": "^5.1.0"
  }
}
