// Stub interfaces for other types, you need to expand them according to your actual requirements

    export interface IAspectsService {
        options: any;  // Specify the actual type if known
        stripingSub: number;
        // ... Add other properties
    }
